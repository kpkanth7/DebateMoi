# Copy this file to secrets.toml for local dev (never commit secrets.toml).
# On Streamlit Community Cloud, add these via the app's Secrets dashboard.

DEEPSEEK_API_KEY = "sk-..."
OPENAI_API_KEY = "sk-..."
